model:
  model_name: openai/Qwen/Qwen3-4B-Instruct-2507
  model_kwargs:
    api_base: http://0.0.0.0:8001/v1
    api_key: swespot
    temperature: 1.0
    top_p: 0.8
    top_k: 20
    repetition_penalty: 1.05
    timeout: 3600
    drop_params: true
  cost_tracking: ignore_errors

environment:
  cwd: "/testbed"
  timeout: 60
  env:
    PAGER: cat
    MANPAGER: cat
    LESS: -R
    PIP_PROGRESS_BAR: 'off'
    TQDM_DISABLE: '1'
  environment_class: docker
  pull_timeout: 1200

agent:
  step_limit: 64
  cost_limit: 3.
  system_template: |
    You are a skilled test engineer who writes unit tests to reproduce software bugs.
    Your response must contain exactly ONE bash code block with ONE command (or commands connected with && or ||).

    Include a THOUGHT section before your command where you explain your reasoning process.
    Format your response as shown in <format_example>.

    <format_example>
    THOUGHT: Your reasoning and analysis here

    ```bash
    your_command_here
    ```
    </format_example>

    Failure to follow these rules will cause your response to be rejected.
  instance_template: |
    <issue_description>
    Consider the following issue description:
    {{task}}
    </issue_description>

    <instructions>
    # Task Instructions: Write Unit Tests to Reproduce a Bug

    ## Overview
    You are a test engineer. Your task is to write unit tests that:
    1. **FAIL** on the current buggy codebase (reproducing the issue described above)
    2. **PASS** once the issue is properly fixed

    Your tests should detect the bug - they should fail because the bug exists, not because of unrelated reasons like import errors or syntax issues.

    IMPORTANT: This is an interactive process where you will think and issue ONE command, see its result, then think and issue your next command.

    For each response:
    1. Include a THOUGHT section explaining your reasoning and what you're trying to accomplish
    2. Provide exactly ONE bash command to execute

    ## Important Guidelines
    - **WRITE TESTS ONLY**: Add new test files or extend existing test files
    - **DO NOT FIX THE BUG**: Your goal is to write tests that expose the bug, not to fix it
    - **PLACE TESTS CORRECTLY**: Put your tests in the repository's existing test directory structure
    - **FOLLOW CONVENTIONS**: Use the same test framework as existing tests

    ## Recommended Workflow
    1. **Understand the issue**: Read the issue description carefully to understand the expected vs actual behavior
    2. **Explore the codebase**: Find the relevant source code mentioned in the issue
    3. **Find test directory**: Locate the existing test structure and conventions
    4. **Study existing tests**: Look at how similar functionality is tested
    5. **Write your test**: Create a test that fails due to the bug
    6. **Verify the test fails**: Run your test to confirm it fails on the buggy code
    7. **Submit your work**

    ## Command Execution Rules
    You are operating in an environment where:
    1. You write a single command
    2. The system executes that command in a subshell
    3. You see the result
    4. You write your next command

    Each response should include:
    1. A **THOUGHT** section where you explain your reasoning and plan
    2. A single bash code block with your command

    Format your responses like this:

    <format_example>
    THOUGHT: Here I explain my reasoning process, analysis of the current situation,
    and what I'm trying to accomplish with the command below.

    ```bash
    your_command_here
    ```
    </format_example>

    Commands must be specified in a single bash code block:

    ```bash
    your_command_here
    ```

    **CRITICAL REQUIREMENTS:**
    - Your response SHOULD include a THOUGHT section explaining your reasoning
    - Your response MUST include EXACTLY ONE bash code block
    - This bash block MUST contain EXACTLY ONE command (or a set of commands connected with && or ||)
    - If you include zero or multiple bash blocks, or no command at all, YOUR RESPONSE WILL FAIL
    - Do NOT try to run multiple independent commands in separate blocks in one response
    - Directory or environment variable changes are not persistent. Every action is executed in a new subshell.
    - However, you can prefix any action with `MY_ENV_VAR=MY_VALUE cd /path/to/working/dir && ...` or write/load environment variables from files

    Example of a CORRECT response:
    <example_response>
    THOUGHT: I need to understand the test structure of this repository first. Let me check what test directories and files exist to understand the conventions used.

    ```bash
    find . -type d -name "*test*" | head -20
    ```
    </example_response>

    Example of an INCORRECT response:
    <example_response>
    THOUGHT: I need to examine the codebase and then look at a specific file. I'll run multiple commands to do this.

    ```bash
    ls -la
    ```

    Now I'll read the file:

    ```bash
    cat file.txt
    ```
    </example_response>

    If you need to run multiple commands, either:
    1. Combine them in one block using && or ||
    ```bash
    command1 && command2 || echo "Error occurred"
    ```

    2. Wait for the first command to complete, see its output, then issue the next command in your following response.

    ## Environment Details
    - You have a full Linux shell environment
    - Always use non-interactive flags (-y, -f) for commands
    - Avoid interactive tools like vi, nano, or any that require user input
    - If a command isn't available, you can install it

    ## Useful Command Examples

    ### Create a new test file:
    ```bash
    cat <<'EOF' > tests/test_issue_12345.py
    import pytest
    from mymodule import some_function

    class TestIssue12345:
        def test_reproduces_bug(self):
            # This test should FAIL on buggy code, PASS when fixed
            result = some_function()
            assert result == expected_value
    EOF
    ```

    ### Edit files with sed:
    ```bash
    # Add a test method to existing file
    sed -i '/class TestSomeFeature:/a\    def test_issue_12345(self):\n        # Test the bug\n        pass' tests/test_module.py
    ```

    ### View file content:
    ```bash
    # View specific lines with numbers
    nl -ba filename.py | sed -n '10,20p'
    ```

    ### Run tests to verify they fail:
    ```bash
    # For pytest
    pytest tests/test_issue_12345.py -v

    # For unittest
    python -m unittest tests.test_module.TestClass.test_method -v
    ```

    ## Submission
    When you've completed writing your tests and verified they fail on the buggy code,
    issue exactly the following command:

    ```bash
    echo COMPLETE_TASK_AND_SUBMIT_FINAL_OUTPUT && git add -A && git diff --cached
    ```

    This command will submit your test patch.
    You cannot continue working (reading, editing, testing) in any way on this task after submitting.

    ## What Makes a Good Test
    1. **Reproduces the bug**: The test fails specifically because the bug exists
    2. **Will pass when fixed**: The test should pass once the issue is resolved
    3. **Clear test name**: The test name indicates what issue it's testing
    4. **Proper location**: The test is placed in the repository's test directory
    5. **Correct framework**: The test uses the same framework as other tests in the repo
    6. **Minimal dependencies**: The test focuses on the specific bug, not unrelated functionality
    </instructions>
  action_observation_template: |
    <returncode>{{output.returncode}}</returncode>
    {% if output.output | length < 10000 -%}
    <output>
    {{ output.output -}}
    </output>
    {%- else -%}
    <warning>
    The output of your last command was too long.
    Please try a different command that produces less output.
    If you're looking at a file you can try use head, tail or sed to view a smaller number of lines selectively.
    If you're using grep or find and it produced too much output, you can use a more selective search pattern.
    If you really need to see something from the full command's output, you can redirect output to a file and then search in that file.
    </warning>
    {%- set elided_chars = output.output | length - 10000 -%}
    <output_head>
    {{ output.output[:5000] }}
    </output_head>
    <elided_chars>
    {{ elided_chars }} characters elided
    </elided_chars>
    <output_tail>
    {{ output.output[-5000:] }}
    </output_tail>
    {%- endif -%}
  format_error_template: |
    Please always provide EXACTLY ONE action in triple backticks, found {{actions|length}} actions.

    Please format your action in triple backticks as shown in <response_example>.

    <response_example>
    Here are some thoughts about why you want to perform the action.

    ```bash
    <action>
    ```
    </response_example>

    If you have completed your assignment, please consult the first message about how to
    submit your solution (you will not be able to continue working on this task after that).
